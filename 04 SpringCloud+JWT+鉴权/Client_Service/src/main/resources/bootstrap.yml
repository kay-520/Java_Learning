server:
  port: 8003
spring:
  application:
    name: client-service
eureka:
  client:
    serviceUrl:
      defaultZone: http://${eureka.host:127.0.0.1}:${eureka.port:8000}/eureka/
  instance:
    prefer-ip-address: true